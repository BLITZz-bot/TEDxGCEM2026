// Client-side direct Supabase access is disabled for maximum security.
// All database queries and authentication checks are now routed through secure Next.js API endpoints.
export function createClient() {
  return null;
}
